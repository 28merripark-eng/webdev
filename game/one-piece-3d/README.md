One Piece 3D — Prototype

How to run:

1. Start a simple HTTP server in the `game/one-piece-3d` folder (recommended).

   Python 3:

   ```bash
   python -m http.server 8000
   ```

2. Open http://localhost:8000 in your browser.

Notes and next steps:
- This is a minimal prototype using Three.js with a placeholder player.
- Next: add character model (GLTF), environment art, animations, combat, enemy AI, and networking.
